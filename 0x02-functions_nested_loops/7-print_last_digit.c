#include <stdio.h>

/**
*print_last_digit - prnts last digit
*@a: the argument to be taken
*Return: returns ld
*/

int print_last_digit(int a)
{
	int ld = a % 10;
	int b = ld;

	return (ld);
	return (b);
}
